# Copy this file to .env and adjust for your environment. Vite only exposes
# variables prefixed with VITE_ to client code (see src/config/api.js).

# Base URL of the Node/Express API. Optional — defaults to
# http://localhost:5000/api when unset, which matches Backend's default PORT.
VITE_API_URL=http://localhost:5000/api
